## 1. Prereqs and audit

- [x] 1.1 Branch from `dev` with the spec's exact name: `git checkout dev; git pull; git checkout -b extract-web-framework-package`.
- [x] 1.2 Inventory the reusable surface in `src/Tjb.Web`: list every file under `Areas/Identity`, `Shared`, `Pages` (`_Host`, `Error`, `EmailTemplates`), `Services`, `Data/RevalidatingIdentityAuthenticationStateProvider*`, and `wwwroot`. Mark each as framework / host / undecided (e.g. `NavMenu` link set — see design Open Question 4). (`RevalidatingIdentityAuthenticationStateProvider` lives under `Areas/Identity/`, not `Data/`.)
- [x] 1.3 Resolve design Open Questions before they block a phase: package names (Q3) before phase 5; generic-vs-concrete user type (Q1) before phase 3; base-context placement (Q2) before phase 1; `NavMenu` ownership (Q4) before phase 1. (Recorded in design.md "Open-Question resolutions".)
- [ ] 1.4 Capture a behavior baseline of the current app to compare against after the refactor: login page render, Google sign-in flow, the post-registration SES confirmation email, and `MigrateAsync` against a fresh local MySQL. **(Needs a local MySQL run — pending operator/local env.)**

## 2. Phase 1 — Carve out the `Tjb.Web.Framework` RCL

- [x] 2.1 Create `src/Tjb.Web.Framework/Tjb.Web.Framework.csproj` as `Microsoft.NET.Sdk.Razor`, `net10.0`, packable, with the package metadata fields (PackageId, Authors, etc.) and the SDK references the UI needs (`Microsoft.AspNetCore.Identity.UI`, `AWSSDK.SimpleEmail`, EF/Pomelo only if the moved code needs it). (No EF/Pomelo needed in the RCL. Added `<Using>` items for the Web SDK's implicit global usings that `Sdk.Razor` omits.)
- [x] 2.2 Move `Areas/Identity/**`, `Shared/**` (`MainLayout`, `LoginDisplay`, `SurveyPrompt`, and `NavMenu` per Q4), `Pages/Error.*`, `Pages/EmailTemplates/**`, `Services/**`, and `Areas/Identity/RevalidatingIdentityAuthenticationStateProvider*` into the RCL. Move `wwwroot/**` reusable assets in. (`_Host.cshtml` kept in host — see design deviation note; `App.razor` likewise.)
- [x] 2.3 Decide and create the Identity-base assembly location (Q2): dedicated `src/Tjb.Web.Framework.Data`. Added to the solution; does not reference `Tjb.Data`. (Class `AwsWebAppIdentityDbContext` implemented in phase 3.)
- [x] 2.4 Add `Tjb.Web.Framework` (and the base-context assembly) to `Tjb.sln`. Reference `Tjb.Web.Framework` from `Tjb.Web` via `ProjectReference`.
- [x] 2.5 Fix up namespaces/`_Imports.razor`/`@using` so the moved Razor content compiles from the RCL. `App.razor`/`_Host.cshtml` (kept in host) resolve the framework `MainLayout` via the preserved `Tjb.Web.Shared` namespace; static-asset links repointed to `_content/Tjb.Web.Framework/`.
- [ ] 2.6 **Checkpoint:** `dotnet run --project src/Tjb.Web` locally; confirm the login page renders, the shared layout + CSS are intact, and `/Identity/Account/Login` serves the framework page. Compare visually to the 1.4 baseline. **(Build + CI unit-test filter green; visual local run pending operator/local MySQL.)**

## 3. Phase 2 — Carve out `Tjb.Web.Hosting` extensions

- [x] 3.1 Create `src/Tjb.Web.Hosting/Tjb.Web.Hosting.csproj` as `Microsoft.NET.Sdk`, `net10.0`, packable, referencing `Tjb.Web.Framework`. Add to `Tjb.sln`. (Uses `FrameworkReference Microsoft.AspNetCore.App` + Identity.UI/EFCore/Google packages; the generic `<TContext>` extensions don't need a reference to `Tjb.Web.Framework.Data`.)
- [x] 3.2 Implement `AddAwsWebAppIdentity<TContext>` (default Identity + EF stores + cookie events + auth-state provider) in namespace `Microsoft.Extensions.DependencyInjection`.
- [x] 3.3 Implement `AddAwsWebAppGoogleAuth<TContext>(IConfiguration)` preserving the OnCreatingTicket/OnRemoteFailure/OnTicketReceived logging, plus the optional `LogAwsWebAppAuthConfig` Google-config-status helper. (Made generic over `TContext` to preserve the OAuth-callback DB-connectivity diagnostic faithfully; design listed it non-generic.)
- [x] 3.4 Implement `AddAwsWebAppEmail(IConfiguration)` (SES options + `IAmazonSimpleEmailService` region-empty-autodetect factory + `IEmailService` + `IViewRenderService` + `AddHttpContextAccessor`).
- [x] 3.5 Implement `UseAwsWebAppForwardedHeaders` (`XForwardedFor | XForwardedProto`, `KnownProxies`/`KnownNetworks` cleared) and `ApplyDatabaseMigrationsAsync<TContext>` (EnsureCreated + Migrate, log-and-swallow). (Event/migration logging switched from `ILogger<Program>` to `ILoggerFactory` category strings — log-category change only, not observable runtime behavior.)
- [x] 3.6 Rewrite `src/Tjb.Web/Program.cs` to: register `TjbDbContext`, call the `Add*` extensions, add the host's own `AddRazorPages`/`AddServerSideBlazor`/`AddHealthChecks`/`WeatherForecastService`, then `UseAwsWebAppForwardedHeaders` + standard pipeline + `MapFallbackToPage("/_Host")` + `await app.ApplyDatabaseMigrationsAsync<TjbDbContext>()`. (Pipeline order — forwarded-headers → `UseAuthentication` → signin-google debug middleware → dev/prod branch — preserved exactly.)
- [ ] 3.7 **Checkpoint:** local run; Google OAuth sign-in completes and the SES confirmation email sends (against a verified sandbox recipient); `dotnet test --filter "FullyQualifiedName!~Tjb.UiTests"` is green. **(Unit-test filter green; OAuth/SES runtime needs a deployed/local env — pending.)**

## 4. Phase 3 — Identity-only base DbContext

- [x] 4.1 Implement `AwsWebAppIdentityDbContext : IdentityDbContext<IdentityUser>` carrying only the Identity-side `OnModelCreating` config (no task entities) in the base-context assembly (`Tjb.Web.Framework.Data`). Abstract base with a `protected AwsWebAppIdentityDbContext(DbContextOptions)` ctor.
- [x] 4.2 Rebuild `Tjb.Data`'s `TjbDbContext` to derive `AwsWebAppIdentityDbContext` and define only the task-management entities; call `base.OnModelCreating(builder)` first. (`OnConfiguring` with `MigrationsAssembly("Tjb.Migrations")` stays on `TjbDbContext` — it's consumer-specific.)
- [x] 4.3 **Checkpoint (hard gate):** Equivalence **proven** — pre-refactor vs post-refactor `TjbDbContextModelSnapshot` are byte-identical (generated by reverting only `TjbDbContext`'s base class and regenerating). The base-context extraction changes nothing in the EF model. NOTE: the literal "empty diff vs the committed migration" couldn't be run — the repo has **no committed `ModelSnapshot`** and the committed `InitialCreate` is **already stale** vs the current task-entity configs (pre-existing drift, unrelated to this change). Probe artifacts + the temporary design-time-factory tweak (offline `ServerVersion.Parse` + `MigrationsAssembly`) were reverted; repo migration dir is back to HEAD. `dotnet-ef` 8.0.27 installed as a local tool (`dotnet-tools.json`).
- [ ] 4.4 Apply migrations against a fresh local MySQL (`dotnet run --project src/Tjb.Migrations`) and confirm the Identity + task tables are created as before. **(Needs a local MySQL — pending operator.)**

## 5. Phase 4 — Package cutover (prove true consumption)

- [x] 5.1 Add `Pack Tjb.Web.Framework.Data`, `Pack Tjb.Web.Framework`, and `Pack Tjb.Web.Hosting` steps to `.github/workflows/zbuild.yml` next to the existing `Pack Tjb.*` steps, following the GitVersion SemVer + branch-suffix convention. Coordinate the edit region with wilhelm's `make-deployment-stack-reusable` pack changes. (Inserted between `Pack Tjb.Migrations` and `Pack Tjb.Web`; `publish-nuget` already globs `./nupkgs/` so no publish-job change.)
- [x] 5.2 `dotnet pack` the projects locally; confirmed the RCL `.nupkg` contains `staticwebassets/` (bootstrap, open-iconic, `site.css`, `favicon.png`), the scoped-CSS bundle, and `build/Microsoft.AspNetCore.StaticWebAssets.props`; Razor pages/components compile into the RCL DLL (expected for an RCL).
- [ ] 5.3 Push the branch; confirm CI publishes all three packages to GitHub Packages with the branch-suffixed version. **(Requires push/CI — not done here; user controls push.)**
- [ ] 5.4 Swap `Tjb.Web.csproj` from `ProjectReference` to `PackageReference` for `Tjb.Web.Framework` + `Tjb.Web.Hosting` (and transitively `Tjb.Web.Framework.Data`), pinned to the just-published branch-suffixed version. Add the GitHub Packages source to `NuGet.config` (currently nuget.org-only). `dotnet restore` from the feed; build. **(Blocked on 5.3.)**
- [ ] 5.5 **Checkpoint:** push and let the branch deploy; confirm the deployed app at `https://extract-web-framework-package.{DOMAIN_NAME}` renders login + layout (validating `_content/` asset paths from the package), Google sign-in works, the SES email sends, and the UI tests pass against the deployed URL. **(Blocked on 5.3/5.4 + deploy.)**

## 6. Phase 5 — Docs, validation, release

- [x] 6.1 Finalize package metadata (id per Q3, description, repository URL, tags) on all three projects. (Mirrors the existing `Tjb.*` csproj metadata; Q3 kept `Tjb.*` prefix.)
- [x] 6.2 Update `README.md` and `CLAUDE.md` to note the web framework now ships as `Tjb.Web.Framework` + `Tjb.Web.Hosting` (+ `Tjb.Web.Framework.Data`) packages and that `Tjb.Web` is their first consumer; cross-linked the roadmap.
- [x] 6.3 Grep audit: `src/Tjb.Web` contains no `Areas/Identity` pages, no `Shared/` layout, and no `Services/` email code of its own. (Confirmed: 0 files in each.)
- [x] 6.4 `openspec validate extract-web-framework-package --strict` → "Change ... is valid".
- [~] 6.5 Verify each scenario in `specs/web-framework-package/spec.md`: RCL UI/static assets resolve (pkg contains `staticwebassets/` under `_content/Tjb.Web.Framework/`) ✓; extensions compose (Program.cs builds against them) ✓; consumer-derivable Identity base + empty model diff ✓ (snapshot equivalence); packages pack ✓ / publish + restore ⧗ (push/CI); non-lossy deploy ⧗ (deploy + UI tests pending push).
- [ ] 6.6 Tag a pre-release version of the packages; record the version for `sample-solution-local` to consume. **(After CI publishes — pending push.)**
- [ ] 6.7 Archive this change per the experimental workflow (`/opsx:archive`) once merged to `dev` and validated. **(Pending merge.)**
